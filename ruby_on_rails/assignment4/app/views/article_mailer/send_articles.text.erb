Hello <%= @current_user.name %>

<% @articles.each do |article| %>
    <%= article.id %>, <%= article.title %>, <%= article.description %>, <%= article.public_flag == true ? "Public" : "Private"  %>
<% end %>